Para ejecutar el código solo se debe ejecutarlo.

Una vez ejecutado se generarán dos figuras,

En la primera figura se presenta la regesión lineal y el valor óptimo del vector theta.
Y en la figura dos se muestra la regresión ponderada localmente, para diferentes valores de tau.
